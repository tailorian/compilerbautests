f(x:int)
    if x # 0 then
        var y : int := 2;
        while y < x do
            var z : int := 0;
            if y # z then
                var u : int := 10;
                y := (x + y) * u;
            else
                y := f(z, y) : int;
            end;
        end;
    end;
end;
